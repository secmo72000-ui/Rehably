/**
 * API Client
 * 
 * Define here:
 * - RequestConfig interface
 * - getAuthToken() helper
 * - buildUrl() helper
 * - request<T>() function with JWT header
 * - api object with: get, post, put, patch, delete methods
 */
