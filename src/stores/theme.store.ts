/**
 * Theme Store (Zustand)
 * 
 * Define here:
 * - CustomColors interface
 * - ThemeState interface
 * - useThemeStore hook with:
 *   - theme (light/dark)
 *   - customColors (primary, secondary)
 *   - setTheme, toggleTheme, setCustomColors actions
 * - Use persist middleware
 */
