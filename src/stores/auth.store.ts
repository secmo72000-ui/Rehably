/**
 * Authentication Store (Zustand)
 * 
 * Define here:
 * - User interface
 * - AuthState interface
 * - useAuthStore hook with:
 *   - user, token, isAuthenticated, isLoading
 *   - setUser, setToken, login, logout actions
 * - Use persist middleware for token storage
 */
